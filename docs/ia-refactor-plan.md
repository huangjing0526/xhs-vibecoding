# 信息架构改造·验收清单（走查版）

版本：v1.0（由 v0.1 施工方案改写而来）
走查日期：2026-08-26 23:30 前后，对照 `localhost:3000`（dev server）+ 主仓库代码实测
目标仓库：`/Users/kp/workspace/projects/内容工作台`
走查时代码基线：已提交至 `f3c252e`；工作区另有改造一的未提交改动（约 20 文件，属并行会话，未触碰）

## 0. 一句话结论

v0.1 方案写下时预设「待确认，未动代码」，实测**七项改造已落地六项**（多数已提交），改造一在并行会话的工作区里实施中且 dev server 已在跑这份代码。本文档职责随之从「施工方案」转为「验收清单」：记录每项的落地证据、与原方案的差异、以及收尾前的遗留项。

---

## 1. 七项改造核对状态

| # | 改造 | 状态 | 关键证据 |
|---|---|---|---|
| 一 | 作品库收视频产物 | 🟡 实施中（未提交） | `lib/works.ts`（`AnyWork`）、`GET /api/works` 聚合、`app/api/video-factory/_works.ts`；UI 实测：作品页「全部/图片/视频」切换，视频卡带时长角标+项目名，按日期分组 |
| 二 | 项目页统一入口 | ✅ 已提交 | 项目页「图文 N / 视频 N」双 lane，视频卡带进度（"5 镜 · 已生成 5 镜"）+更新日期，点卡跳视频工厂；⌘K 分组「项目」可搜到视频项目；全局「创建」= 图文笔记/视频项目 两项菜单 |
| 三 | 目录收编 needsNote | ✅ 已提交 | `capabilities.ts` 已无 `needsNote`；工具目录 7 个工具，无一张点进去要「先选一篇笔记」 |
| 四 | 统一叫「项目」 | ✅ 基本落地 | `AREAS.note.label` =「项目详情」；空态「还没有图文项目」。残留见 §3-1 |
| 五 | 分区按产出物重排 | ✅ 已提交 | `ToolCategory` = 做图/做视频/沉淀模板/复盘；模板页右上「拆一条」+「拆博主」都在 |
| 六 | 首页减脂 | ✅ 已提交 | 首页无「推荐/全部」tab，工具区一屏平铺；`HomeHub` 已无"推荐"字样 |
| 七 | 素材页产出承接 | ✅ 已提交 | `Notice.action` 已存在；生成选题成功发「已生成 N 条选题…」+「看全部项目」按钮（`WorkflowDashboard.tsx:775`） |

### 与原方案的实质差异（均为合理简化，无需返工）

- **改造一取流**：未新建 `GET /api/video-factory/clip-file`，复用现有 `video-factory/clip` 路由。目的一致，少一个端点。
- **改造一删除策略**：如方案所定——视频第一期不提供删除，「存入资产库」仅图片。
- **改造七文案**：实际为「已生成 N 条选题并写回飞书，已打开第一条」+「看全部项目」，比方案的「去项目页看」承接更进一步（直接打开第一条）。

---

## 2. 验收标准核对

| 标准 | 结果 |
|---|---|
| 1. 工具目录无「先选一篇笔记」卡 | ✅ 实测通过（7 工具逐个看过目录卡） |
| 2. 视频成片在作品库可见、可播、可溯源 | 🟡 可见+可溯源（项目名/时长/日期）实测通过；**内联播放未实测** |
| 3. 视频项目在项目页可见、可点续作；⌘K 可搜 | ✅ 实测通过（搜"牛仔"命中视频项目，点卡进视频工厂） |
| 4. 首页无 tab、无重复入口 | ✅ 实测通过 |
| 5. `workbench-taxonomy.md` 逐句核对无一句与界面不符 | ⏳ 待改造一收尾——该文档在并行会话工作区里正被修改，等提交后再逐句核对 |

---

## 3. 遗留项（收尾清单）

1. **文案残留**：笔记详情页左侧窄列标题仍是「笔记 N」（`components/workflow/NoteList.tsx:257`，sidebar variant）。改造四的最后一处，一行改动。
2. **改造一收尾**：等并行会话提交后，补验收标准 2 的内联播放 + 标准 5 的 taxonomy 逐句核对。
3. **附带发现·非阻塞**：
   - dev server（16:48 启动）对 23:24 的提交出现过 HMR 陈旧 bundle——首次进项目页渲染旧版文案，整页刷新后恢复。看到旧文案先刷新，不是代码问题。
   - 走查期间弹过一次「工作流启动失败，已切换 Demo 模式」toast，Next dev overlay 报 2 Issues。可能与陈旧 bundle 有关，也可能是飞书工作流调用失败，未深查；改造一收尾时顺带看一眼 console。

---

## 4. 原方案存档说明

v0.1 的问题分析、方案细节与实施顺序已被实现超越，原文存档在同目录 `ia-refactor-plan-v0.1-archive.md`（v0.1 从未提交过 git，勿删存档）。原「明确不做的事」仍然有效，照录如下：

- 不合并两套存储——图文在飞书、视频/图片在本机，只做统一视图
- 不动 `xhsWorkflow → contentWorkflow` 改名（多平台 Phase 2e 已推迟）
- 不改飞书表结构
- 不删任何 `AreaId`——所有区仍可被 ⌘K 和意图路由直达
